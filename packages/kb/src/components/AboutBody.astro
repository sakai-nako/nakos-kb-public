---
// 入力は About の本文 Markdown だけ。描画は renderMarkdown のサニタイズを通した HTML に限る。
import { renderMarkdown } from '../shared/markdown';
const { body } = Astro.props;
const html = renderMarkdown(body);
---
<div class="about-body" set:html={html} />
